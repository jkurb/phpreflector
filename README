Что это такое?

OrmManager - инструмент разработчика, позволяющий делать синхронизацию сущностей
  в виде таблиц БД и файлов классов.

  Две команды:
  	pull <table> <file> - получить измений в файл
  	push <file> <table> - забрать измения из таблицы

Что есть?

- Гибкая генерация класса сущности по таблице с использованием шаблонов.

- Генерация таблицы по сущности класса.

- Получение изменений таблица -> класс (изменяются только поля помеченные как @column):

	- измение названия сущности, комментария

	- добавление, удаление поля

	- изменение названия, комментария, типа, дефолтных значений и опций поля

	- автоподстановка имен констант в дефолтные значения

- Получение изменений  класс -> таблица

	- измение названия сущности, комментария

	 - добавление, удаление поля

	 - изменение комментария, типа, дефолтных значений и опций поля

- Поддержка наследования классов

- Интеграция в IDE (external tools)


TODO:

1. Покрыть юнит тестами
	BaseTemplateHandler.php
	OrmManagerTest.php
		добавление/удаление/изменение поля
	Field.php

2. Сделать php-типы у не полей БД (пока @string)

3. Замена названий переменных в методах

4. Порядок полей

5. Смена названия поля из класса